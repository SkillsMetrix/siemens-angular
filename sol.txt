

import { TestBed } from '@angular/core/testing'
import { AppComponent } from '../app.component'
import { TestcompComponent } from './testcomp.component'


// create test suite
describe('Test Suite for component',()=>{
    beforeEach(()=> TestBed.configureTestingModule({
       // declarations:[TestcompComponent]
    }))
    // test case
    it('should load the component',()=>{
           const fixture= TestBed.createComponent(TestcompComponent)
           const app= fixture.componentInstance;
           expect(app).toBeTruthy()
    })
    it('should test  the  value of component',()=>{
        const fixture= TestBed.createComponent(TestcompComponent)
        const app= fixture.componentInstance;
        expect(app.username).toBe('admin')
 })
 it('should check the template',()=>{
    const fixture= TestBed.createComponent(TestcompComponent)
    fixture.detectChanges()
    const compiled= fixture.nativeElement as HTMLElement
     expect(compiled.querySelector('.content span')?.textContent).toContain('welcome')
})
})
